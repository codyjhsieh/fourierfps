{
  "name": "house-that-remembers",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "THE HOUSE THAT REMEMBERS — a first-person procedural Fourier puzzle game in the browser.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "smoke": "node tools/smoke.mjs"
  },
  "dependencies": {
    "three": "^0.169.0",
    "zustand": "^5.0.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
